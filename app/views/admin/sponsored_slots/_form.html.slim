= simple_form_for [:admin, sponsored_slot] do |f|
  = f.error_notification
  = f.association :app, collection: App.order(:name), include_blank: false
  = f.input :starts_at, required: true
  = f.input :ends_at, required: true

  .mt-4
    = f.button :submit
